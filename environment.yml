name: cv_fitness
channels:
  - conda-forge
  - defaults
dependencies:
  - dash
  - jupyter
  - matplotlib
  - numpy
  - opencv=4.5
  - pandas
  - pip
  - pycodestyle
  - pylint
  - python=3.9
  - scipy
  - pip:
    - mediapipe
    - -e .

prefix: $CONDA_PREFIX\envs\cv_fitness
